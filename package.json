{
  "name": "cmdb-project-ttech",
  "version": "1.0.0",
  "description": "Plataforma empresarial de CMDB y Gestión de Activos Tecnológicos TTECH",
  "scripts": {
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,cyan\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build": "cd frontend && npm run build && cd ../backend && npm run build",
    "start": "cd backend && npm run start",
    "seed": "cd backend && npm run seed"
  },
  "keywords": ["cmdb", "asset-management", "ciberseguridad", "ttech"],
  "author": "TTECH",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
